
const Home = () => {
    // const {name, age} = useContext(AuthContext)
    return (
        <div>
            i am home
            {/* <p>Name: <span>{name}</span></p>
            <p>Age: <span>{age}</span></p> */}
        </div>
    );
};

export default Home;